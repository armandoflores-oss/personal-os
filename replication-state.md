# replication-state.md — Build checklist and resume point

<!-- The single source of truth for build progress. Any session resuming the build:
     1. Read self-improving-os-bootstrap.md (the spec) and config/os-config.md (the parameters).
     2. Read this file to find the current phase.
     3. Never start a phase until the previous phase's acceptance test PASSED.
     Update this file in the same commit as any build step it records. -->

Last updated: 2026-09-01 by Claude (session: Phase 2 built, awaiting next-session acceptance).

## Status legend
`DONE` passed acceptance · `IN PROGRESS` being built · `BLOCKED` waiting on user/external · `PENDING` not started

## Phases

### Phase 0 — Interview → `DONE`
Interview completed 2026-08-18, answers in `config/os-config.md`. OQ-1 resolved: firewalled content sealed in tree. OQ-2 resolved: memory tree in Spanish.
- **Acceptance:** config written and confirmed by Armando. ✅

### Phase 1 — Foundation: the cache repo → `DONE`
Done 2026-08-18: local git repo initialized (branch main); full folder structure; memory tree with per-folder CLAUDE.md in Spanish; `scripts/lib/` (constants with FIREWALLED_DOMAINS in code, events, replay failing loudly on unknown kinds, codes) + `scripts/task.py` CLI; stdlib secret-scan pre-commit hook via core.hooksPath (gitleaks unavailable: no brew, and no binary downloads through the TLS-intercepted office network — swap in gitleaks later from a clean network).
- **Acceptance (local half):** ✅ test event T-1 appended → snapshot rebuilt → rendered view showed it → closed with evidence; unknown event kind rejected loudly; hook blocked a staged fake AWS key.
- **Acceptance (remote half):** ✅ 2026-09-01, on a clean network (github.com served a genuine Sectigo cert, not the FortiGate one; ports 22 and 443 both open). Account `armandoflores-oss` (personal), private repo `personal-os`, ed25519 key `SHA256:hiroBHS5sPL57QqSQZRn7CG8ye/lHaW6lGlMMTUQkII`, remote over SSH. Pushed `f63d19b`; clean clone into a temp dir diffed byte-identical against the working tree (41 files both sides); `git pull --ff-only` clean.
- **Defect found and fixed during acceptance:** `core.hooksPath` lives in `.git/config` and does NOT survive a clone — the secret-scan hook files travel, their activation does not, so a fresh clone would commit with no scanning at all, silently. Added `scripts/setup.sh` (idempotent, POSIX, verifies rather than assumes). Verified on the throwaway clone: after bootstrap the hook blocked a staged fake AWS key. **Phase 7 must assert `core.hooksPath` is set on every machine** — this is exactly a rule-10 abandoned-bot failure.
- **Account hygiene:** GitHub primary email moved to `armandofr@gmail.com` (verified); the Draiver address and its Google sign-in connection removed so the company cannot password-reset into this account. Commit identity is repo-local (`user.email=armandofr@gmail.com`), git global left with no identity.

### Phase 2 — Feedback capture: the loop's mouth → `IN PROGRESS (built, acceptance pending)`
Always-on instruction (project CLAUDE.md + skill): scan every user message for action codes, corrections, durable facts, status changes, new contacts → append events, update memory, commit, end reply with a compact receipt. Capture first, then answer.
- Built 2026-09-01: `FEEDBACK_KINDS` + `CARD_FOLDER` + firewall routing in `scripts/lib/constants.py`; `scripts/lib/feedback.py` (append-only feedback log, card writer that appends and never rewrites); `scripts/capture.py` (`mark` / `signal` / `receipt`). The always-on instruction lives at `~/.claude/CLAUDE.md` (user scope, so it loads in every session regardless of cwd); a copy is tracked here at `docs/instruccion-captura.md` — **the copy is documentation, the live file is `~/.claude/CLAUDE.md`; edit both or they drift.** Phase 7 should assert they match.
- The receipt is rendered by `capture.py receipt` from the two logs, never composed by hand, so it cannot claim a write that did not happen.
- Dry run on a throwaway copy: correction + status + contact + commitment + task create all captured from one message; a `fw-finanzas` signal was forced into `memory/privado/` and appeared in the receipt as a count only.
- **Acceptance (half 1 — same session):** ✅ a correction produces a receipt carrying the event and its lesson line.
- **Acceptance (half 2 — NEXT session):** ✅ 2026-09-02 — asked unprompted for standing draft rules and both corrections from the previous day were applied without a reminder.
- **Code scheme (2026-09-02):** `CODE_PREFIXES` T/A/P in constants; `next_code(prefix)` mints per namespace in ONE log, so codes are a display concern and merges stay trivial. Added event kinds `veto` (permanent, rule 5) and `apply_proposal`. `scripts/reply.py` parses the ten terse forms — grammar in code, and an unrecognised reply raises instead of guessing, because a misread reply writes a wrong event and that is worse than no event. All ten exercised on a throwaway clone.
- **Owed to Phase 3:** the brief renderer must print the code at the head of every replyable line, and `veto` must permanently exclude an item from ever being proposed again.

### Phase 3 — Ingest + the brief: heartbeats → `IN PROGRESS (both built, 3-morning acceptance pending)`
Cloud ingest routine (several times/day, watermarked, single-writer on `cache/`): classify new email/calendar/transcript metadata, mint task candidates only from explicit commitments, noise gate in code, dedupe-at-creation, outbound-leg reconcile of sent messages. Daily brief at 07:00 America/Mexico_City (cron in UTC — convert, mind DST): Python renderer reading only the repo; sections per spec; every actionable line carries its T-/A-/P- code. Verify each connector with a real read before trusting it. Verify plan's runs/day allowance (~12–14 needed).
- **Ingest built 2026-09-02.** `scripts/lib/gate.py` (noise gate: meta -> exploratory -> commitment, then dedupe on action+counterparty) with 9 tests in `scripts/test_gate.py`; `scripts/ingest.py` (watermarks in `state/watermarks.json`, suppression log in `state/suppressed.ndjson`, receipts in `syncs/`, cap of 12 creates/run); outbound reconcile closes on counterparty match + >=2 distinctive title tokens.
- **Two bugs found by the dry run, both fixed:** `replay` was dropping `action`/`counterparty` from the create payload, which silently broke cross-run dedupe AND the outbound reconcile; and action extraction matched whole words, so "confirmes" and "confirmar" were different actions and the same promise could land twice.
- **The scheduler is NOT a cloud runner.** `mcp__scheduled-tasks` runs on this Mac while the app is open (missed runs fire at next launch), and its cron is LOCAL time, not UTC. Consequences: (a) the config's "cloud routine" framing is wrong for this tool; (b) the single-writer story is safe, since the laptop is the only writer; (c) an intermittently-open laptop means ingest is really catch-up-on-wake. Schedule set: `0 9,12,15,17 * * 1-5` local, plus a few minutes of scheduler jitter.
- **Verified, closing a config TODO:** America/Mexico_City has had NO DST since 2022 — UTC-6 in January and July alike. The "verify DST handling at Phase 3" note is resolved: no drift.
- **Working hours confirmed by Armando 2026-09-02: 08:30-19:00, Mon-Fri.** Schedule widened to `0 9,11,14,16,18 * * 1-5` (5 runs/day). Source: `memory/context/jornada-laboral-de-armando-08-30-19-00-cdmx.md`.
- **Documented hazard:** the 18:00 local run is 00:00 UTC the next day. Harmless while this scheduler evaluates cron in LOCAL time; if anything here ever moves to a UTC cron, that run's day-of-week must shift to `2-6` or every Friday evening run is silently skipped.
- **Hard constraint from Armando (2026-09-01): no brief item may exceed two lines.** Enforce it in the renderer — truncate or split there, never rely on a prompt to be brief. Source rule: `memory/rules/brief-maximo-dos-lineas-por-item.md`.
- **Brief built 2026-09-02.** `scripts/brief.py` renders from the repo ONLY; the routine deposits calendar into `cache/calendar/` and transcripts into `cache/transcripts/` first, which is what keeps the renderer's input boundary honest. Sections in Armando's order; two-line cap enforced by `cap()`; every replyable line carries its code; noise counted, never listed. Routine `brief-personal-os`, cron `0 7 * * 1-5` local.
- **Two bugs the populated dry run exposed, both fixed:** (a) an item that was new AND cross-check-suppressed vanished from the brief entirely — it left the standing list for being fresh, then was dropped from the new list; the new section is now computed first and suppressed items fall back into standing; (b) `is_duplicate` treated two tasks that both lacked action/counterparty as duplicates of each other, so any hand-created task suppressed the next one.
- **Standing rule added to `~/.claude/CLAUDE.md`:** never render the brief conversationally as a substitute for the real one.
- **First live morning FAILED, 2026-09-03.** Both routines fired on time (brief 07:15, ingest 09:09) and both died at their FIRST connector call: the scheduled-run harness tells the model "the user is not present", so a connector permission prompt has nobody to answer and the run is cut off mid-tool-call. Nothing was written — no brief, no cache, no receipt, no error. Fixed by making failure loud rather than by removing it: each routine now writes a `heartbeat/start` line to `syncs/` BEFORE touching any connector and a `heartbeat/end` at the very end, and `brief.py health()` reports any task that started and never finished. The remaining root cause needs Armando: click "Run now" once per task and approve the connector prompts, which stores the approvals on the task.
- **Root cause found 2026-09-03 (second diagnosis; the first was wrong).** The runs do not die — they HANG. `ps` showed four live routine processes burning CPU, launched with `--permission-mode default --permission-prompt-tool stdio --disallowedTools AskUserQuestion`: they do request approval, but over stdio with no UI attached, so the prompt has nowhere to appear and the process waits forever. That is why "Run now" appeared to do nothing and why the heartbeat fix never fired — it sits behind the first tool call. Compounding it, the 81 approvals already in `settings.local.json` point at OLD connector UUIDs (Gmail was `mcp__2371861f-…`, today it is `mcp__18fee21b-…`), so reinstalling a connector silently orphans every approval it had.
- **Fix:** pre-granted permissions in `~/.claude/settings.json` (backup at `settings.json.bak`, copy tracked at `docs/settings-permisos.json`). Armando chose unrestricted `Bash` after being shown the risk; connectors are read-only, with an explicit `deny` list for every send/create/delete tool rather than relying on omission.
- **Phase 7 must watch:** (a) connector UUIDs drifting away from the allow-list, which silently disarms every approval; (b) routine processes alive with no matching receipt.
- **Lesson for Phase 7:** an unattended routine that can die between its trigger and its first write is invisible to every check that reads only outputs. Liveness has to be proved at the start of a run, not inferred from its results.
- **First real end-to-end run, 2026-09-03 15:36 CST.** Pipeline works: 46 Gmail items -> 3 tasks, 43 suppressed (45 sin_compromiso, 1 duplicado, 1 exploratorio); 5 Drive items -> 1 task; outbound reviewed 1, closed 0; brief rendered to `briefs/2026-09-03.md`. Five bugs the run exposed, all fixed the same day:
  1. **The brief denounced itself.** It writes its own `heartbeat/start` before rendering and its `end` after, so at render time its own end is always missing — every brief opened with a false "died mid-run" alarm. Now only starts older than 2h are reported.
  2. **Every calendar event got the same prep note.** Card slugs matched on "armando", and Armando attends all his own meetings. Self-identity tokens are now excluded and a match needs two slug words, not one.
  3. **The meeting cross-check suppressed everything.** It matched 2 tokens against ALL transcripts concatenated; against a big enough haystack any two common words hit. Now each transcript is checked separately and 60% of the title's distinctive tokens must appear in ONE of them.
  4. **Calendar notifications became tasks.** "Canceled event with note: …" minted T-5. Google's event notifications are now META: they talk about a meeting, they are nobody's commitment.
  5. **A transcript became a task named after its file.** T-6 was titled "BCA Europe Requirements - Notes by Gemini" with counterparty `gemini-notes`. Non-mail sources now title the task with the commitment sentence, and the note-taker is never a counterparty.
- T-5 and T-6 were archived through `reply.py`. **Still wrong and unfixed:** T-4 "penske case study for EU" carries counterparty `kavak` — the extractor takes the first known client name anywhere in the body, including quoted threads and signatures.
- **Delivery was missing entirely until 2026-09-03.** The brief rendered to `briefs/` and its run output sat in the Routines history — a place Armando would have to go look, which is the review-queue failure rule 2 exists to prevent. He chose Slack DM over email: same reach on his phone, but a narrower blast radius if the routine ever misbehaves, since nothing can leave for a client. Routine step 4 now DMs the script's literal output to `U077GQC532M`; `slack_send_message` is allowed, and Slack's create/schedule tools are explicitly denied. Verified end to end 2026-09-03 16:33 CST.
- **Residual risk, stated plainly:** a permission rule cannot constrain a recipient. Allowing send is allowing send anywhere; only the routine's prompt names U077GQC532M as the sole destination. Phase 7 should assert that no Slack message left for any other channel.
- **2026-09-04: the brief routine rewrote its own SKILL.md mid-run.** Its reasoning was correct — a Slack DM you send to yourself raises no notification, so yesterday's delivery was silent — but an unattended routine that can edit the file governing it has no limits, only suggestions. Blocked in code: `Write`/`Edit` denied on `~/.claude/scheduled-tasks/**`, `~/.claude/settings.json` and `~/.claude/CLAUDE.md`. The prompt now states the rule and the reason, and instructs the routine to REPORT a disagreement rather than act on it.
- **Delivery is now threefold:** the routine response (readable in Routines), the Slack DM (the consultable copy), and a `PushNotification` one-liner — the only leg that actually reaches Armando away from the Mac.
- **Approval prompts were caused by three gaps,** found by diffing tools actually invoked against the allow-list: `get_message` (Gmail, legitimate — now allowed), plus `slack_read_channel` and writes outside the repo. The last two were not gaps to fill but scope creep: the brief routine was reading Gmail and Slack channels its prompt never mentioned. Left denied, and the prompt now forbids both explicitly.
- **Method worth reusing:** to find why an unattended run prompts, list the tools its transcript actually called and subtract the allow-list. Guessing at the cause cost two wrong diagnoses on 2026-09-03.
- **2026-09-04, the real cause of the approval prompts.** Not a missing permission: `cd <path> && git ...` trips a harness safety check of its own ("can execute untrusted hooks from the target directory"), which an allow rule for `Bash` does not clear — and this repo does have hooks, so the warning is correct. Every run stopped there for an approval nobody could give, which is also why the 09:00/11:00/14:00 ingests left no heartbeat: the heartbeat was chained after that `cd`. Fixed by removing the pattern: `scripts/heartbeat.py` replaces the inline one-liner (rule 8 — it should never have been in a prompt), every command uses an absolute path, and git runs as `git -C <repo>`, which never changes the shell's directory. Specific `Bash(...)` allow rules added for those exact commands.
- **Confirmed limitation, not a bug:** local routines only run while the Mac is awake with the app open. On 2026-09-04 the 07:00 brief actually rendered at 08:55 (when the laptop woke) and three ingest slots were simply missed. Either the machine stays awake, or the brief arrives when Armando opens it, or this moves to something that genuinely runs in the cloud — a redesign, not a setting.
- **2026-09-04: routines were signing their own events as `actor: "user"`.** `capture.py` and `task.py` both defaulted `--actor` to `user`, so a routine writing a feedback card produced an event indistinguishable from something Armando said — including one `kind: correction` recording the routine's OWN decision to change its delivery. The spec warns about exactly this: the Phase 6 grader treats user events as ground truth and would have counted the system's work as Armando intervening, punishing itself for running. `--actor` is now REQUIRED in both scripts (they exit non-zero without it) and both routine prompts state the rule and the reason.
- **Three distinct harness checks prompt an unattended run**, none of them cleared by a `Bash` allow rule: (1) `cd <path> && git ...` — untrusted hooks in the target directory; (2) heredocs and braces-with-quotes — shell expansion obfuscation; (3) chained `&&`. All three are now forbidden in both prompts, with the reason, and JSON is written with the Write tool instead of a shell heredoc.
- **Also visible in the routine-authored commits: scope creep.** The brief routine read Gmail and Slack channels it was never asked to touch, and grepped the codebase for `def cmd_reconcile` to debug reconcile behaviour. Useful work came out of it (a real analysis of the AVASA/Hertz thread on T-3, and the fact that draiver.com and driverdo.com are one mailbox), but an unattended routine that widens its own remit is the same failure class as one that rewrites its own instructions.
- **Regression introduced and fixed the same hour (2026-09-04).** Making `--actor` required was right, but it was added to task.py's TOP-LEVEL parser, so `task.py rebuild` — which signs nothing and is invoked by subprocess from ingest.py and reply.py — started failing too, breaking the whole pipeline. `--actor` now lives on the writing subcommands (`create`, `event`) only. Lesson: a guardrail added to a shared entry point reaches callers that were never meant to satisfy it; the smoke test has to exercise the subprocess paths, not just the CLI surface.
- **Four distinct approval mechanisms, found one at a time on 2026-09-04** — none of them cleared by a `Bash` allow rule, and each needing a different fix: (1) `cd <path> && git ...`, the repo-hooks check; (2) heredocs and braces-with-quotes, the shell-obfuscation check; (3) chained `&&`/`;`; (4) any path outside the working directory, including `/tmp`, which is a working-directory check that permission rules cannot lift at all. Fixes: `git -C`, the Write tool for files, one command per call, and scratch files in `cache/tmp/` inside the repo (gitignored). A `Write(/tmp/**)` rule was removed as useless.
- **Writing permission rules by hand failed twice**, both times because the guess was wrong rather than the idea: the rules were unquoted while every real command quotes the repo path (it has spaces), and a bare `"Bash"` entry does not grant blanket approval. The reliable path is the dialog's own "Permitir siempre", which stores the exact command against the routine. Prefer it over hand-written rules.
- **2026-09-04, the approval churn and its real cause.** Armando spent an afternoon clicking permission dialogs and said plainly it was unsustainable. He was right, and most of it was self-inflicted: approvals are stored against the exact command string, and every prompt edit that day changed the commands, silently voiding every approval he had already granted. Guessing at hand-written allow rules made it worse (twice wrong: unquoted paths against a repo whose name has spaces, and a bare `"Bash"` entry that grants nothing).
- **Structural fix: `scripts/routine.py`.** Each routine now issues exactly TWO fixed shell commands — `routine.py <ingest|brief>-pre` and `-post` — with everything deterministic behind them (heartbeats, pull, the three ingest legs, render, commit, cleanup of intermediates). Four stable command strings across both routines instead of a dozen that changed with every edit. The point is not fewer approvals but STABLE ones: approve these once and they keep working, because editing a prompt no longer changes what runs.
- **Both routines are PAUSED** pending one supervised "Run now" each to bank the approvals.
- **Rule for future changes:** editing a routine prompt in a way that changes a command string costs Armando a fresh round of approvals. Change `routine.py` instead; the prompt should only ever describe which connectors to call.
- **2026-09-04, the routines improvise and prompt edits do not stop them.** During a supervised "Run now" the brief routine repeatedly tried `cd .../cache/transcripts && for f in ...; head -c 200` — inspecting files nobody asked it to inspect — and RETRIED the same command after Armando denied it, twice, in two variants. Telling it not to, in the prompt, did not hold.
- **Blocked in code instead:** `permissions.deny` now refuses `Bash(cd *)`, `for`, `ls`, `cat`, `head`, `tail`, `grep`, `find`. Deny is evaluated before allow, so this holds regardless of what any prompt says or how a model reasons around it. The legitimate routines only ever run `python3 ".../routine.py" <phase>`, so nothing real is lost. This is rule 8 taken to its conclusion: the guardrail belongs in code, and a prompt that has to be obeyed is not a guardrail.
- **Cost to note:** this also constrains interactive sessions, which used `cd <repo> && ...` freely. Absolute paths and `git -C` replace it, which is better practice anyway.
- **Permission-rule syntax, learned the hard way (three wrong guesses on 2026-09-04).** (1) A bare `"Bash"` entry grants nothing; use the prefix form `Bash(python3 *)`. (2) Rules must match the command as actually issued, quotes included — this repo's path has spaces, so every real command quotes it. (3) **File-tool rules take absolute paths with a DOUBLE leading slash**: `Write(//Users/.../**)`, not `Write(/Users/.../**)`. The single-slash rules never matched, which is why the same Write prompted repeatedly even after "Permitir siempre". The settings schema shows the form in its own example (`Edit(//etc/*)`) and I did not read it closely enough.
- **Standing lesson:** verify a permission rule by observing whether the prompt stops, not by assuming the syntax. Each wrong guess cost Armando a round of dialogs.
- **Write prompts were never fixed by a permission rule.** Neither `Write(/abs/**)` nor `Write(//abs/**)` stopped them, and "Permitir siempre" did not persist across runs either. After four wrong guesses at rule syntax — each one costing Armando another round of dialogs — the honest move was to stop guessing: `permissions.defaultMode` is now `acceptEdits`, which accepts file writes without asking. Bash rules and the whole deny list are untouched, so the routines still cannot run `cd`/`ls`/`cat`, send mail, or delete in Drive.
- **Cost, stated:** this loosens file writes in every session, not just the routines. Narrow it later if a rule form that actually works is found; verify by watching whether the dialog stops, never by reading the syntax.
- **Definitive on the Write prompts (2026-09-04 18:32):** `acceptEdits` was written to settings at 18:31:09 and the run that started at 18:32:45 — 96 seconds later, so it did read it — still prompted. The scheduler launches every routine with `--permission-mode default` on the command line, and that flag beats `permissions.defaultMode`. No settings change can silence a Write inside a routine. Command approvals from "Permitir siempre" DO persist (ingest-pre/post stopped asking); Write approvals do not. That asymmetry is a product behaviour, not a configuration error.
- **Mitigation, since elimination is impossible:** the ingest now writes ONE combined `cache/tmp/ingest.json` (`{gmail, drive, sent}`) instead of three files, and `routine.py` splits it. Three interruptions per run become one. The two command strings are unchanged, so no approval had to be re-granted. Still open: cutting the ingest from five runs/day to two, which would take the daily dialog count from ~15 to 2.
- **Root cause of the approval fatigue, found by reading what the harness itself writes (2026-09-04).** `~/.claude/settings.local.json` is where "Permitir siempre" persists a rule. It holds 133 entries, and among them: all four `routine.py` commands, stored as EXACT full command strings including quotes — so commands are genuinely banked and will not be asked again. But there is **not a single `Write(...)` entry**, despite the user clicking "Permitir siempre" on Write dialogs roughly six times, and the file's mtime predates most of those clicks. **Clicking "always" on a file-write dialog persists nothing.** That is a harness behaviour, not a misconfiguration.
- Also learned from that file: the harness writes its own file rules with a DOUBLE leading slash (`Read(//Applications/**)`), confirming the syntax, and it writes them to `settings.local.json` rather than `settings.json`. Matching rules were added there in that exact form — untested as of this writing.
- **Confirmed NOT the cause:** there is no org policy on this machine (no `managed-settings.json` in either location), so nothing needs to be requested from an administrator.
- **Acceptance:** three consecutive mornings of briefs needing no structural correction; replying "close <code>" works via Phase 2; no rendered item exceeds two lines.

### Phase 4 — Memory: nightly session mining → `PENDING`
Local deterministic courier (catch-up-on-wake — this laptop is intermittent): digest the day's session transcripts, scrub secret-shaped strings, exclude firewalled-folder sessions, push to `cache/sessions/<host>/`. Nightly cloud distiller: extract corrections/facts/status/contacts/commitments, dedupe, write through Phase 2 rails, delete processed digests, receipt + heartbeat, per-run cap above daily volume.
- **Acceptance:** a mid-session correction from yesterday, never explicitly saved, is applied in today's behavior and shown in the distiller receipt.

### Phase 5 — The second brain → `PENDING`
(a) Obsidian vault pointed at `memory/`, `home.md` map of content, git plugin only. (b) Density: deterministic weaver backfills `[[wikilinks]]`, topic hubs (client & deal history, people, competitive intel, pricing/tarifas per config §8), no orphans, capped links. (c) Capture lanes: paste-in-session, self-mailed links with keyword, watched folder → immutable `sources/` + distilled `context/` pages. (d) Payoff: producers consult the wiki before drafting (rule 14); `changelog.md` appended nightly. No weekly learning report ever (rule 2).
- **Acceptance:** three-hop graph navigation project→counterparty→competitor; a link self-mailed yesterday is a linked wiki page this morning; a routine draft visibly cites something never said in that session.

### Phase 6 — The loop: reconcile, grade, self-retire → `PENDING`
Nightly cloud routine after data-in, before brief: prepare (deterministic evidence gathering) → judge (model: hard closes / soft proposals / drop ambiguous) → apply (guardrails in code: id validation, firewall demotion, ~15 mutation cap, idempotency stamp, proposals ledger with 72h aging and permanent vetoes) → grade (deterministic 0-10, idempotent, distinguishes user events from system events). Weekly pass: promote recurring corrections into rules (one commit each); low-risk upgrades auto-apply, structural ones render as numbered proposals in the brief. Self-retirement at 21+ days of trend data.
- **Acceptance:** within the first week: ≥1 evidence-based close with correct receipt, zero resurfaced closed items, eval score visible in the brief.

### Phase 7 — Watchdog → `PENDING`
Manifest of every producer AND every mandatory phase within each; stdlib-only checker (always exits 0): heartbeats, named log-line assertions (amber within 24h if missing), parked state with reason/since/next-check auto-expiry, freshness from content never mtime, schedule-aware thresholds, credential-expiry countdowns. Brief banner + session-start hook only when non-green.
- **Acceptance:** stop one producer for a day → next brief says so unprompted. Delete one phase from a routine prompt → brief flags the missing capability within a day.

## Resume notes
- Repo root is this folder: `~/Documents/Claude Personal Improvement/`. Git repo on branch `main`, remote `origin` = `git@github.com:armandoflores-oss/personal-os.git` (SSH only — never HTTPS, see config §network note).
- **Next action: confirm Phase 2's second half next session** (does the capture rule hold unprompted?), then Phase 3.
- On any fresh clone, run `sh scripts/setup.sh` first — the pre-commit hook is inert until you do.
- `gitleaks` is still a stdlib stand-in; swap it in from a clean network when convenient.
- Pushing is gated by the Claude Code auto-mode classifier: Claude can commit but not push. Either Armando runs `git push` or a `Bash(git push:*)` permission rule gets added.
- Design rules in Part 2 of the spec are non-negotiable; when in doubt, re-read rule 2 (single pane), rule 8 (thin prompts, fat scripts), rule 9 (watchdog capabilities), rule 10 (abandoned-bot test).
